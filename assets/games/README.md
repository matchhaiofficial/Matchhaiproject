# Game artwork placeholders

Add the PNG artwork for each competitive game here. The filenames referenced in
`src/constants/games.ts` should match the assets you drop into this folder
(e.g. `cs2.png`, `valorant.png`, `fc25.png`, `tekken8.png`).

Using this README keeps the directory committed without bundling invalid image
references when the files are still missing.
